apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: springboot-monitor
  labels:
    release: monitoring

spec:
  selector:
    matchLabels:
      metrics: enabled

  endpoints:
    - port: http
      path: /actuator/prometheus
      interval: 15s