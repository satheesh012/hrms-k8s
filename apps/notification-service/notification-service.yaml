apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: hrms
  labels:
    metrics: enabled
spec:
  selector:
    app: notification-service
  ports:
    - name: http
      port: 8087
      targetPort: 8087
  type: ClusterIP