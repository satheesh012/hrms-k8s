apiVersion: v1
kind: Service
metadata:
  name: service-registry
  namespace: hrms
  labels:
    metrics: enabled
spec:
  selector:
    app: service-registry
  ports:
    - name: http
      port: 8761
      targetPort: 8761
  type: ClusterIP